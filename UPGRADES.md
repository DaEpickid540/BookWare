# BookWare — Upgrades Audit

_Generated by autonomous scan. All items tracked for implementation._

---

## 🔴 Missing Features (spec gaps)

| # | File | Issue | Fix |
|---|---|---|---|
| 1 | `teacher.js` / `teacher.html` | No rental request / approval workflow — checkouts are instant, teachers can't approve or deny | Add `requireApproval` toggle per teacher; student submits request → teacher approves/denies → checkout happens on approval |
| 2 | `teacher.js` / `teacher.html` | Export is `.MD` only — spec requires **CSV** | Add CSV export button alongside .MD |
| 3 | `teacher.js` / `teacher.html` | Invite page has no **QR code** | Generate QR code via chart.googleapis.com and display in invite output panel |
| 4 | `teacher.js` / `teacher.html` | No **email share** button on invite output | Add `mailto:` button that composes an email with the invite link in body |
| 5 | `student.js` / `student.html` | No "**Request to rent**" flow — tied to approval workflow above | Show "Request Checkout" when teacher has `requireApproval: true` |
| 6 | `student.js` / `student.html` | No **rental request status** view — students can't see pending/approved/denied requests | Add "Pending Requests" section to Locker page |
| 7 | `admin.js` / `admin.html` | Admin cannot see **all libraries** — only users | Add Libraries page listing all teacher libraries with their books |
| 8 | `admin.js` / `admin.html` | Admin cannot see **all rentals** across all teachers | Add Rentals page with global checkout history |
| 9 | `admin.html` | Admin settings is a flat stack, not **organized panels** | Group into collapsible/card panels: System, Users, Danger Zone |

---

## 🟠 UX Problems

| # | File | Issue | Fix |
|---|---|---|---|
| 10 | `teacher.js` | `prompt()` for class name, `confirm()` for deletions — browser dialogs, no styling | Replace with custom inline form for create class; styled modal for confirm |
| 11 | `student.js` | `confirm()` for return flow — blocking browser dialog | Replace with custom confirm modal |
| 12 | `teacher.js` | `prompt()` for rename class | Replace with inline edit-in-place (click name → input → save) |
| 13 | `teacher.js` | `alert()` for maintenance message | Replace with styled banner / redirect |
| 14 | `student.html` | No visual feedback when selecting a library chip — only colour change, no transition | Add subtle `transform: scale(1.02)` and transition on `.library-chip.selected` |
| 15 | `student.js` | Book list re-renders entirely on every search keystroke | Debounce to 150ms (currently instant DOM updates on in-memory array — fine, but could flash) |
| 16 | `teacher.js` | Class code UI buries the code inside a card — hard to see at a glance | Increase code font size, add copy-on-click for the whole code-box |
| 17 | `all` | No empty-state illustrations — just grey text | Add SVG spot illustrations to key empty states (no books, no students, no history) |
| 18 | `student.html` | Wishlist heart and Recommend star have no count indicators | Show number of students who've wishlisted / recommended (aggregate) |
| 19 | `teacher.js` | Bi-weekly banner cannot be dismissed until next cycle — `localStorage.setItem(KEY, Date.now())` runs immediately, so reopening won't show it again | Move timestamp write to Dismiss click only |

---

## 🟡 Performance Issues

| # | File | Issue | Fix |
|---|---|---|---|
| 20 | `teacher.js` `loadCheckedOut()` | N+1 Firestore reads: fetches student names with one `getDoc` per checked-out book inside a `for` loop | Batch via `Promise.all()` or cache student names at load time |
| 21 | `teacher.js` `loadRoster()` | N+1 inside N+1: per-class roster loads hit Firestore once per class, then per student for each getDocs | Already a single getDocs per class — acceptable, but should cache student data |
| 22 | `student.js` `renderNotifications()` | Runs on init and fetches 3+ Firestore reads synchronously on every page load | Cache notification data for 60s, use `sessionStorage` |
| 23 | `admin.js` `loadFirestoreStats()` | 4 parallel collection queries on every page switch to debug | Add a "Refresh" button, don't auto-run on every debug page visit |
| 24 | `student.js` `renderAddedTeachersList()` | Sequential `getDoc` per added teacher in a for-of loop | Convert to `Promise.all()` |
| 25 | All | No `loading="lazy"` on some dynamically-injected `<img>` tags (book covers injected via innerHTML) | Add `loading='lazy'` to all cover img strings (partially done, needs audit) |

---

## 🔵 Accessibility Gaps

| # | File | Issue | Fix |
|---|---|---|---|
| 26 | `student.js` | `renderBooks()` — action buttons have no accessible name beyond icon + text, but icons use `class='bi bi-heart-fill'` without `aria-hidden` when injected via innerHTML | Add `aria-hidden='true'` to all inline icons in JS-generated HTML |
| 27 | `admin.html` | Ban modal has no focus trap — keyboard users can tab outside the modal | Add focus trap: capture Tab key in modal, cycle through focusable elements |
| 28 | `all` | Skip-to-main link missing on all pages | Add `<a href="#main-content" class="skip-link">Skip to main content</a>` |
| 29 | `app.css` | Focus rings are visible but only 2px — WCAG recommends 3px minimum for AA | Increase to 3px |
| 30 | `student.html` / `teacher.html` | Sidebar toggle `aria-expanded` is set on click but sidebar state is not announced via live region | Add `aria-live="polite"` status on sidebar collapse |
| 31 | `admin.js` | User table has no `<caption>` element — screen readers won't describe table purpose | Add `<caption>` to the users table |
| 32 | `all` | `<button>` elements that open panels have no `aria-expanded` state | Wire `aria-expanded` on collapsible sections |

---

## 🟢 Code Quality

| # | File | Issue | Fix |
|---|---|---|---|
| 33 | `student.js` + `teacher.js` | Theme/brightness code extracted to `theme.js` ✅ but `fmtDate()`, `esc()`, `toast()` are duplicated in both files | Move shared utilities to a `utils.js` module |
| 34 | `student.js` | `renderBooks()` is 120+ lines — builds complex HTML with 6 conditional buttons inline | Extract into `buildBookCard(book, opts)` helper |
| 35 | `teacher.js` | `renderLibraryList()` is 70+ lines building HTML inline | Same as above |
| 36 | `theme.js` | Exports `initARIA` but admin doesn't use ARIA — admin still imports `initTheme` but would call a no-op `initARIA` if it did | No-op is harmless but documents the split should be cleaner |
| 37 | `admin.js` | `forceLogoutAll()` shows a toast saying it would need Cloud Functions but does nothing — misleading | Either implement via Cloud Functions or remove the button |
| 38 | `teacher.js` | `checkBiweeklyNotification()` calls `show()` via `setTimeout(show, 1800)` — the 1.8s delay is arbitrary and relies on `allBooks` being loaded by then | Make it wait for `allBooks` explicitly |

---

## 🔐 Security / Hardening

| # | File | Issue | Fix |
|---|---|---|---|
| 39 | `firebase.json` | `script-src` has `'unsafe-inline'` — prevents CSP from blocking injected scripts | Move to nonce-based CSP (complex for Firebase Hosting static sites) or at minimum document this risk |
| 40 | `firestore.rules` | No `requests` collection rule yet — will be needed once request flow is added | Add rule (being done in Task 2) |
| 41 | `teacher.js` | Invite tokens are stored as Firestore doc IDs — if someone enumerates doc IDs, they find tokens | Generate tokens as `crypto.randomUUID()` separately from the doc ID, store as a field — already done correctly ✅ |
| 42 | `admin.js` | Auto-ban logic writes to Firestore from client — a malicious user could trigger this from devtools | This is low risk since they'd just ban themselves, but the write path should be a Cloud Function in production |

---

## 📱 Mobile / Responsive

| # | File | Issue | Fix |
|---|---|---|---|
| 43 | `app.css` | On screens <640px, sidebar is always collapsed (icon only) — no way to see nav labels | Add a slide-in drawer with backdrop overlay for mobile |
| 44 | `app.css` | `.book-search-grid` doesn't collapse on mobile — 200px min-width causes overflow on 320px screens | Change to `minmax(160px, 1fr)` |
| 45 | `app.css` | `.stats-grid` (4-column) doesn't reduce below 2 columns on very small screens | Add `@media (max-width:420px) { .stats-grid { grid-template-columns: 1fr 1fr; } }` (already there) |
| 46 | `admin.css` | Admin sidebar is always visible — on mobile it pushes content off-screen | Hide admin sidebar on mobile, add hamburger menu |
| 47 | `app.css` | `split-grid` collapses to 1 column at 900px ✅ but `.ban-form` doesn't wrap cleanly on mobile | Add `flex-wrap: wrap` (already there) — check `.text-input--sm` doesn't force layout breaks |

---

## ✨ Nice-to-Have / Future

| # | Description |
|---|---|
| 48 | Dark/light mode toggle in top bar (one click, without going to Settings) |
| 49 | Book detail modal — click a book to see full description, page count, publication year |
| 50 | Reading streak / gamification for students |
| 51 | Teacher can add a personal note to a book recommendation |
| 52 | Student can leave a review / rating on a returned book |
| 53 | Bulk actions in teacher library (delete multiple books, recommend multiple) |
| 54 | Export student reading history to PDF (currently .MD and .CSV only) |
| 55 | Push notifications via Firebase Cloud Messaging (infrastructure exists in rules) |
| 56 | Search across all libraries at once (global search) |

---

_Items 1–9 are being implemented in Task 2. Items 10–47 are partially addressed. See CHANGES.md for progress._
